import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  image: {
    width: "100%",
    resizeMode: "contain",
    height: 150,
    marginTop: 20,
  },
  content: {
    marginHorizontal: 40,
  },
});
